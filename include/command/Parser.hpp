#pragma once

#include <iostream>
#include <sstream>
#include <string>

#include <vector>
#include <map>

#include "network/Server.hpp"

class Parser
{
    private:
        Server* _srv;
        std::map<std::string, Command *>
}